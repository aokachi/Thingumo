<%= form_with(model: @special_answer, local: true) do |form| %>
  <%= form.text_area :content %>
  <%= form.hidden_field :user_id, value: current_user.id %>
  <%= form.hidden_field :post_id, value: @post.id %>
  <%= form.submit "送信する" %>
<% end %>
<%= link_to 'キャンセル', post_path(@post) %>